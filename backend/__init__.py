"""Backend package for the Ascend Flask app.

The legacy entrypoint is still app.py; modules here are extracted services
that let us shrink that file incrementally without changing public behavior.
"""
